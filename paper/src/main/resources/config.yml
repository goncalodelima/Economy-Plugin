database:
  host: "localhost"
  port: 3306
  user: root
  password: "INSERT_YOUR_KEY_HERE"
  database: smp
  file: ""

hikari:
  # Sets the maximum size of the MySQL connection pool.
  # - Basically this value will determine the maximum number of actual
  #   connections to the database backend.
  # - More information about determining the size of connection pools can be found here:
  #   https://github.com/brettwooldridge/HikariCP/wiki/About-Pool-Sizing
  maximumPoolSize: 10

  # Sets the minimum number of idle connections that the pool will try to maintain.
  # - For maximum performance and responsiveness to spike demands, it is recommended to not set
  #   this value and instead allow the pool to act as a fixed size connection pool.
  #   (set this value to the same as 'maximumPoolSize')
  minimumIdle: 10

  # This setting controls the maximum lifetime of a connection in the pool in milliseconds.
  # - The value should be at least 30 seconds less than any database or infrastructure imposed
  #   connection time limit.
  maximumLifetime: 1800000 # 30 minutes

  # This setting controls how frequently the pool will 'ping' a connection in order to prevent it
  # from being timed out by the database or network infrastructure, measured in milliseconds.
  # - The value should be less than maximum-lifetime and greater than 30000 (30 seconds).
  # - Setting the value to zero will disable the keepalive functionality.
  keepAliveTime: 0

  # This setting controls the maximum number of milliseconds that the plugin will wait for a
  # connection from the pool, before timing out.
  connectionTimeout: 5000 # 5 seconds

# Messaging service for cross-server transactions.
#
# Use BungeeCord or Redis to handle cross-server communication.
#
# Notes:
# - If a player sends money to another player on a different server, timing issues may occur.
# - If the sender logs out before the database operation is completed, the receiver may not be notified.
#   ⚠️ This only happens when using BungeeCord as the messaging service.
# - When using BungeeCord, Redis is required to update the cache for pending transactions
#   and ensure consistency across servers.
#
# Options:
# - "BungeeCord" → uses BungeeCord messaging for cross-server notifications (requires Redis for pending transactions)
# - "Redis" → requires the plugin to be installed on the proxy; ensures instant and reliable updates
# - "none" → disables sending money to players on other servers
messaging-service: "none" # BungeeCord, Redis, or none